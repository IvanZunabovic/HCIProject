import React from 'react';
import Layout from '../components/layout';

const Store = () => {
    return (
        <Layout>
            <h1>Store</h1>
        </Layout>
    );
};

export default Store;