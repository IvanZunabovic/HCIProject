import React from 'react';
import Layout from '../components/layout';

const Courses = () => {
    return (
        <Layout>
            <h1>Courses</h1>
        </Layout>
    );
};

export default Courses;