import React from 'react';
import Layout from '../components/layout';

const Events = () => {
    return (
        <Layout>
            <h1>Events</h1>
        </Layout>
    );
};

export default Events;