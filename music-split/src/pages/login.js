import React from 'react';
import Layout from '../components/layout';

const Login = () => {
    return (
        <Layout>
            <h1>Log in</h1>
        </Layout>
    );
};

export default Login;